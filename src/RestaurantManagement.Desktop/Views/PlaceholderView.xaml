<UserControl x:Class="RestaurantManagement.Desktop.Views.PlaceholderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="700">
    <UserControl.Resources>
        <SolidColorBrush x:Key="CardBackground" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#0F172A"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#64748B"/>
        <SolidColorBrush x:Key="BorderLight" Color="#E2E8F0"/>
    </UserControl.Resources>

    <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
        <Border Background="{StaticResource CardBackground}" CornerRadius="16" 
                BorderBrush="{StaticResource BorderLight}" BorderThickness="1"
                Padding="40" MaxWidth="540" HorizontalAlignment="Center">
            <StackPanel HorizontalAlignment="Center">
                <Border Background="#F1F5F9" CornerRadius="32" Width="64" Height="64" HorizontalAlignment="Center" Margin="0,0,0,16">
                    <TextBlock Text="{Binding Icon}" FontSize="32" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <TextBlock Text="{Binding Title}" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                <TextBlock Text="{Binding Description}" FontSize="13" Foreground="{StaticResource TextSecondary}" TextAlignment="Center" TextWrapping="Wrap" Margin="0,0,0,20" LineHeight="18"/>
                <Border Background="#EEF2FF" CornerRadius="8" Padding="12,6" HorizontalAlignment="Center">
                    <TextBlock Text="{Binding MilestoneTarget, StringFormat='Scheduled for {0}'}" FontSize="12" FontWeight="SemiBold" Foreground="#4F46E5"/>
                </Border>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
